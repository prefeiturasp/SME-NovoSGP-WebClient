import styled from 'styled-components';
import { Base } from '~/componentes/colors';

export const ContainerTagGrafico = styled.span`
  background-color: ${Base.Azul};
  border-radius: 4px;
  color: ${Base.Branco};
  font-weight: bold;
  padding: 3px 5px 3px 5px;
  font-size: 12px;
`;
