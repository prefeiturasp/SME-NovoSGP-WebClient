test('TODO: Este componente esta com problemas para ser testado, voltar nele depois', () => {
  expect(() => {
    // eslint-disable-next-line @typescript-eslint/no-var-requires
    const Component = require('.').default;
  }).not.toThrow();
});
