import React from 'react';
import { render } from '@testing-library/react';
import LabelSemDados from './labelSemDados';
test('renders LabelSemDados without crashing', () => {
  render(<LabelSemDados />);
});
